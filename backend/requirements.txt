fastapi>=0.110
uvicorn>=0.29
supabase>=2.4
# deps.py imports supabase_auth.errors directly (local JWT verification via
# GoTrueClient.get_claims) — was previously only present transitively via
# supabase itself.
supabase-auth>=2.10
python-dotenv>=1.0
httpx>=0.27
stripe>=10.0
# courts/parse-causelist takes multipart file uploads (FastAPI's File/Form) —
# without this, the app crashes on import, not just at request time.
python-multipart>=0.0.9
# court_lookup.py imports this directly for live DRT fetches — was previously
# only present by luck as a transitive dependency of another package.
requests>=2.31
# Windows lacks system tzdata — court_lookup uses ZoneInfo("Asia/Kolkata").
tzdata>=2024.1
# Rate limiting (app/rate_limit.py) — in-memory backend, no Redis needed.
slowapi>=0.1.9
# pillow>=10.0
